{{ form_start(formFooter) }}
{{ form_errors(formFooter) }}
{# <div class="row mt-5 d-flex justify-content-center"> #}
    {# <div class="card mb-3 text-center col-6 bg-dark" style="max-width: 50rem;"> #}
        {# <div class="card-header">Footer</div>
        <div class="card-body"> #}

                <div class="row mb-4">
                <div class="col">
                <div class="form-outline form-white">
                    {{ form_widget(formFooter.adresse, {'attr': {'class': 'form-control'}}) }}
                    <label class="form-label" for="footer_adresse">Adresse</label>
                </div>
                </div>
            </div>

            <!-- Email input -->
            <div class="form-outline mb-4 form-white">
                    {{ form_widget(formFooter.email, {'attr': {'class': 'form-control'}}) }}
                    <label class="form-label" for="footer_email">Email</label>
            </div>

            <!-- Password input -->
            <div class="form-outline mb-4 form-white">
                    {{ form_widget(formFooter.tel, {'attr': {'class': 'form-control'}}) }}
                    <label class="form-label" for="footer_tel">Tel</label>
            </div>

            <button class="btn btn-block btn-primary">{{ button_label|default('Sauvegarder') }}</button>


        {# </div>
    </div> #}
{# </div> #}
{{ form_end(formFooter) }}
